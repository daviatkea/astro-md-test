---
import BaseLayout from '../layouts/Base.astro'
import Exercise from "../components/Exercise.astro";
const localData = await Astro.glob('../exercises/*.md');
---
<!DOCTYPE html>

<BaseLayout title="Grid">
    {localData.map((ex) => (
        <Exercise data={ex} />
    ))}
</BaseLayout>